[tool.poetry]
name = "ksink"
version = "0.1.0"
description = ""
authors = ["David Palmer <david.palmer@adscale.co.nz>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
